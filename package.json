{
  "name": "hov-player-ranking",
  "version": "1.0.0",
  "author": "sijink",
  "workspaces": [
    "pkg/data",
    "pkg/ui",
    "pkg/migrate-old-snapshots"
  ],
  "scripts": {
    "check": "npm run check --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "format": "npm run format --workspaces --if-present"
  },
  "devDependencies": {
    "@eslint/js": "^9.29.0",
    "@tsconfig/recommended": "^1.0.10",
    "@types/node": "^20.19.1",
    "esbuild": "^0.25.5",
    "eslint": "^9.29.0",
    "mdt2json": "^1.1.0",
    "prettier": "^3.6.1",
    "tablemark-cli": "^3.0.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.35.0"
  }
}
